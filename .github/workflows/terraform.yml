name: learn terraform workflow
on:
  push:
    branches:
      - main
permissions:
  id-token: write
  contents: read
jobs:
  terraform:
    name: Set up terraform
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v5
      - name: Set up Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: 1.14.4
      - name: Set up aws credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: arn:aws:iam::506768526374:role/learn_docker
          aws-region: eu-north-1

      - name: Initialize Terraform
        run: terraform init
      - name: Plan Terraform
        run: terraform plan
        env:
          TF_VAR_RENDER_API_KEY: ${{secrets.RENDER_API_KEY}}
          TF_VAR_RENDER_OWNER_ID: ${{secrets.RENDER_OWNER_ID}}
          TF_VAR_GHCR_USER_NAME: ${{secrets.GHCR_USER_NAME}}
          TF_VAR_GHCR_AUTH_TOKEN: ${{secrets.GHCR_AUTH_TOKEN}}
          TF_VAR_POSTGRES_DB: ${{secrets.POSTGRES_DB}}
          TF_VAR_POSTGRES_USER: ${{secrets.POSTGRES_USER}}
          TF_VAR_SECRET_KEY: ${{secrets.SECRET_KEY}}

#      - name: Wait for approval
#        uses: trstringer/manual-approval@v1.9.1
#        with:
#          approvers: {{secrets.GHCR_USER_NAME}}
#          minimum-approvals: 1
#          secret: ${{secrets.GITHUB_TOKEN}}

      - name : apply Terraform
        run : terraform apply -auto-approve
        env:
          TF_VAR_RENDER_API_KEY: ${{secrets.RENDER_API_KEY}}
          TF_VAR_RENDER_OWNER_ID: ${{secrets.RENDER_OWNER_ID}}
          TF_VAR_GHCR_USER_NAME: ${{secrets.GHCR_USER_NAME}}
          TF_VAR_GHCR_AUTH_TOKEN: ${{secrets.GHCR_AUTH_TOKEN}}
          TF_VAR_POSTGRES_DB: ${{secrets.POSTGRES_DB}}
          TF_VAR_POSTGRES_USER: ${{secrets.POSTGRES_USER}}
          TF_VAR_SECRET_KEY: ${{secrets.SECRET_KEY}}


